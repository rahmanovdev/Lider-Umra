export namespace LESSONS {
   export interface ITEM {
      id: number;
      title: string;
      rich: string;
      created_at: string;
      video_url: string;
      youtube_id: string;
      youtube_embed_url: string;
      thumbnail_url: string;
   }
}
